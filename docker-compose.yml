services:
  bot:
    build: .
    container_name: xuibot
    restart: always
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - /etc/x-ui/x-ui.db:/etc/x-ui/x-ui.db
    network_mode: host
