services:
  bot:
    build: .
    container_name: 3xui-bot
    restart: always
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - /etc/x-ui/x-ui.db:/etc/x-ui/x-ui.db:ro
    network_mode: host
